export default (currentMont: string | undefined) => { return ""};

//   export default async (currentMont: string | undefined) => {
//     const response = await fetch('http://localhost:3333/schedule')
//     return await response.json()
// };
