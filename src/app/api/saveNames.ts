export default async (position: string, name: string) =>{
  try {
    // const response = await fetch('https://api.github.com/users/lucasfauster')
  // const user = await response.json()
  } catch (error) {
    console.log("Deu ruim "+error);
    return;
  }
}